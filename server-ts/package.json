{
  "private": true,
  "scripts": {
    "init": "yarn global add typescript && yarn install && node init-types.js",
    "start": "tsc -w",
    "compile": "yarn install && node init-types.js && tsc"
  },
  "dependencies": {
    "@hinny/core": "^0.0.3",
    "@hinny/data-jdbc": "^0.0.3",
    "@hinny/meta-data": "^0.0.3",
    "@hinny/mvc": "^0.0.3"
  }
}